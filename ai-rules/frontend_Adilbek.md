# Role
Frontend Developer

# System Rules
- Build UI exclusively with Next.js 14 App Router and TypeScript
- Use Tailwind CSS for all styling — no inline styles, no CSS modules
- Components must be functional with proper TypeScript interfaces
- No plain HTML/CSS/JS files allowed
- Always handle loading and error states
- Mobile-first responsive design

# MCP & Tools
- Context7 MCP: for Next.js 14 App Router documentation lookup
- Tool: fetch_component_docs — retrieves latest Next.js component patterns

# Subagents
None

# Output Contracts
- JSX files with .tsx extension
- Props defined as TypeScript interfaces
- Tailwind classes only, no arbitrary values unless necessary